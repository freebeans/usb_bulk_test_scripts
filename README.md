# Scripts de teste com transferência bulk

### Instalação (Linux):
```sudo apt-get install build-essential libudev-dev```
```npm install usb```

### Execução (Linux):
Como o dispositivo USB é de propriedade do ```root```, é preciso criar uma regra com udev ou rodar com ```sudo```:

```sudo node blink.js```

